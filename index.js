const driver = 'bob';
// Strings can be logged in the [browser] console
console.log("this code was called");
// Variables can be logged in the console
console.log(driver);
// Console.log can take multiple arguments
console.log(driver, 'is the driver variable name');
